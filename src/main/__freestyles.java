import java.util.Random;

import static app.codge.utilities.Utils.*;
import app.codge.utilities.Utils;

public class __freestyles {

    private static void idks() {
        String question = "Is it possible to freestyle a programming language?";
        Random rand = new Random();
        int javaIsAwesome = 3;

        int num = rand.nextInt(10);
        if (num == rand.nextInt(5) + 3) {
            String hi = question + javaIsAwesome;
        } else {
            Utils fiji = new Utils();
            newline();
            long nUm = (byte) fiji.hashCode();

        }
    }

}
